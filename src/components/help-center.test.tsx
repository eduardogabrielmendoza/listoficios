import { render,screen } from "@testing-library/react"; import userEvent from "@testing-library/user-event"; import { describe,expect,it,vi } from "vitest"; import { HelpCenter } from "@/components/help-center";
describe("HelpCenter",()=>{it("filtra preguntas",async()=>{const user=userEvent.setup();render(<HelpCenter/>);await user.type(screen.getByPlaceholderText("Buscar una pregunta…"),"otro servicio");expect(screen.getByText("¿Qué pasa si mi oficio no aparece?")).toBeInTheDocument();expect(screen.queryByText("¿Listoficios cobra por buscar?")).not.toBeInTheDocument();});it("registra una consulta local",async()=>{vi.spyOn(globalThis.crypto,"randomUUID").mockReturnValue("abcde000-0000-4000-8000-000000000000");const user=userEvent.setup();render(<HelpCenter/>);await user.type(screen.getByLabelText("Nombre"),"Ana López");await user.type(screen.getByLabelText("Correo"),"ana@correo.com");await user.selectOptions(screen.getByLabelText("Tema"),"Cuenta y acceso");await user.type(screen.getByLabelText("¿En qué podemos ayudarte?"),"Necesito ayuda para recuperar el acceso a mi cuenta local.");await user.click(screen.getByRole("button",{name:"Registrar consulta"}));expect(await screen.findByText("Consulta registrada")).toBeInTheDocument();expect(localStorage.getItem("listoficios:support-tickets:v1")).toContain("Ana López");});});
