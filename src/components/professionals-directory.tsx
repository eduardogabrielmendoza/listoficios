"use client";
import { FormEvent, useState } from "react";
import Link from "next/link";
import { useRouter, useSearchParams } from "next/navigation";
import { Icon } from "@/components/icons";
import { ProfessionalCard } from "@/components/professional-card";
import type { PricingMode, ServiceProfile } from "@/lib/app-types";
import { categories } from "@/lib/mock-data";
import { BELLA_VISTA_ZONES } from "@/lib/profile-defaults";

export function ProfessionalsDirectory({initialData,total}:{initialData:ServiceProfile[];total:number}) {
  const params = useSearchParams(); const router = useRouter();
  const [q, setQ] = useState(params.get("q") ?? ""); const [mobileFilters, setMobileFilters] = useState(false);
  const category = params.get("category") ?? ""; const zone = params.get("zone") ?? "";
  const pricing = (params.get("pricing") ?? "") as PricingMode | "";
  const sort = (params.get("sort") as "relevance" | "rating" | "price") || "relevance";
  const results = initialData;
  function setParam(key: string, value: string) { const next = new URLSearchParams(params.toString()); if (value) next.set(key, value); else next.delete(key); router.push(`/profesionales?${next}`); }
  function submit(event: FormEvent) { event.preventDefault(); setParam("q", q.trim()); }
  const filters = <div className="grid gap-6"><Filter title="Categoría"><select className="field-input" value={category} onChange={(e) => setParam("category", e.target.value)}><option value="">Todos los oficios</option>{categories.map((c) => <option key={c.id} value={c.id}>{c.name}</option>)}</select></Filter><Filter title="Zona"><select className="field-input" value={zone} onChange={(e) => setParam("zone", e.target.value)}><option value="">Toda Bella Vista</option>{BELLA_VISTA_ZONES.map((z) => <option key={z}>{z}</option>)}</select></Filter><Filter title="Precio"><select className="field-input" value={pricing} onChange={(e) => setParam("pricing", e.target.value)}><option value="">Cualquier modalidad</option><option value="from">Desde un precio</option><option value="hourly">Por hora</option><option value="fixed">Precio fijo</option><option value="quote">A convenir</option></select></Filter><button onClick={() => router.push("/profesionales")} className="secondary-button">Limpiar filtros</button></div>;
  return <><section className="border-b border-[var(--line)] bg-[var(--paper)] px-4 py-12 sm:px-6 lg:px-8"><div className="mx-auto max-w-[1180px]"><p className="section-kicker">Directorio local</p><div className="mt-3 flex flex-col justify-between gap-5 lg:flex-row lg:items-end"><div><h1 className="text-4xl font-semibold tracking-[-.045em] sm:text-6xl">Profesionales en Bella Vista</h1><p className="mt-3 text-[var(--muted)]">Buscá por oficio, nombre o lo que necesitás resolver.</p></div><form onSubmit={submit} className="flex w-full max-w-xl gap-2 rounded-2xl border border-[var(--line)] bg-white p-2"><label className="flex min-w-0 flex-1 items-center gap-2 px-3"><Icon name="search" className="size-5 text-[var(--brand)]"/><span className="sr-only">Buscar profesionales</span><input value={q} onChange={(e) => setQ(e.target.value)} className="min-w-0 flex-1 bg-transparent text-sm outline-none" placeholder="Ej. plomero, humedad, Mariana…"/></label><button className="primary-button !h-11">Buscar</button></form></div></div></section><main className="px-4 py-10 sm:px-6 lg:px-8"><div className="mx-auto max-w-[1180px]"><div className="mb-7 flex items-center justify-between gap-4"><p className="text-sm text-[var(--muted)]"><strong className="text-[var(--ink)]">{total}</strong> resultados</p><div className="flex gap-2"><button onClick={() => setMobileFilters(true)} className="secondary-button !h-10 lg:hidden">Filtros</button><label className="flex items-center gap-2 text-sm text-[var(--muted)]">Ordenar <select value={sort} onChange={(e) => setParam("sort", e.target.value)} className="rounded-full border border-[var(--line)] bg-white px-3 py-2 font-medium text-[var(--ink)]"><option value="relevance">Relevancia</option><option value="rating">Valoración</option><option value="price">Menor precio</option></select></label></div></div><div className="grid gap-8 lg:grid-cols-[250px_1fr]"><aside className="hidden rounded-[24px] border border-[var(--line)] bg-white p-5 lg:block"><h2 className="mb-6 text-lg font-semibold">Filtrar resultados</h2>{filters}</aside><section>{results.length ? <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{results.map((p) => <ProfessionalCard key={p.id} professional={p}/>)}</div> : <div className="rounded-[28px] border border-dashed border-[#b9cec7] bg-white px-6 py-16 text-center"><Icon name="search" className="mx-auto size-7 text-[var(--brand)]"/><h2 className="mt-4 text-xl font-semibold">No encontramos esa combinación</h2><p className="mt-2 text-sm text-[var(--muted)]">Probá quitar un filtro o contanos qué servicio falta.</p><Link href="/ayuda" className="mt-5 inline-flex font-semibold text-[var(--brand)]">No encontré mi servicio →</Link></div>}</section></div></div></main>{mobileFilters && <div className="fixed inset-0 z-50 bg-black/30 lg:hidden"><button className="absolute inset-0" onClick={() => setMobileFilters(false)} aria-label="Cerrar filtros"/><aside className="absolute bottom-0 left-0 right-0 max-h-[85vh] overflow-auto rounded-t-[30px] bg-white p-6"><div className="mb-6 flex justify-between"><h2 className="text-xl font-semibold">Filtros</h2><button onClick={() => setMobileFilters(false)} aria-label="Cerrar"><Icon name="x" className="size-5"/></button></div>{filters}</aside></div>}</>;
}
function Filter({ title, children }: { title: string; children: React.ReactNode }) { return <label className="field-label">{title}{children}</label>; }
