import type { Metadata } from "next"; import { Suspense } from "react"; import { ProfessionalsDirectory } from "@/components/professionals-directory"; import { listProfessionals } from "@/data/professionals"; import type { PricingMode } from "@/lib/app-types";
export const metadata: Metadata = { title: "Profesionales en Bella Vista | Listoficios", description: "Buscá y compará servicios locales por oficio, zona y modalidad." };
export const dynamic = "force-dynamic";
export default async function ProfessionalsPage({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}) { const params=await searchParams; const pick=(key:string)=>typeof params[key]==="string"?params[key] as string:""; const pricing=pick("pricing") as PricingMode|""; const sort=pick("sort") as "relevance"|"rating"|"price"; const result=await listProfessionals({query:pick("q"),category:pick("category"),zone:pick("zone"),pricing,sort:sort||"relevance"}); return <Suspense fallback={<div className="mx-auto max-w-[1180px] p-10">Cargando profesionales…</div>}><ProfessionalsDirectory initialData={result.data} total={result.total}/></Suspense>; }
