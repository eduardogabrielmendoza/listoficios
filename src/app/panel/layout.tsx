import Link from "next/link";import { redirect } from "next/navigation";import { PanelNav } from "@/components/panel-nav";import { requireServerSession } from "@/lib/auth-server";
export const dynamic="force-dynamic";
export default async function PanelLayout({children}:{children:React.ReactNode}){let session;try{session=await requireServerSession();}catch{redirect("/ingresar?next=/panel");}return <main className="bg-[var(--paper)] px-4 py-8 sm:px-6 lg:px-8 lg:py-12"><div className="mx-auto max-w-[1180px]"><header className="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="section-kicker">Mi espacio</p><h1 className="mt-2 text-4xl font-semibold tracking-[-.045em] sm:text-5xl">Hola, {session.user.name.split(" ")[0]}</h1><p className="mt-3 text-sm text-[var(--muted)]">Administrá tu cuenta y tu presencia profesional.</p></div>{session.user.role==="admin"&&<Link href="/admin" className="secondary-button">Administración</Link>}</header><div className="grid gap-6 lg:grid-cols-[220px_1fr]"><PanelNav/><section className="min-w-0 rounded-[28px] border border-[var(--line)] bg-white p-5 sm:p-8">{children}</section></div></div></main>}
