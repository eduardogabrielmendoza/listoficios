import Link from "next/link";import { listNotifications } from "@/data/community";import { requireServerSession } from "@/lib/auth-server";
export default async function NotificationsPage(){const session=await requireServerSession();const rows=await listNotifications(session.user.id);return <><h2 className="text-2xl font-semibold">Avisos</h2><p className="mt-2 text-sm text-[var(--muted)]">Cambios de publicación, moderación y opiniones.</p><div className="mt-7 grid gap-3">{rows.length?rows.map((item)=><article key={item.id} className={`rounded-2xl border p-5 ${item.readAt?"border-[var(--line)]":"border-[#9acabd] bg-[#f2faf7]"}`}><h3 className="font-semibold">{item.title}</h3><p className="mt-2 text-sm leading-6 text-[var(--muted)]">{item.body}</p><div className="mt-3 flex items-center justify-between gap-3"><time className="text-xs text-[var(--muted)]">{item.createdAt.toLocaleDateString("es-AR")}</time>{item.href&&<Link href={item.href} className="text-xs font-semibold text-[var(--brand)]">Abrir →</Link>}</div></article>):<div className="rounded-2xl border border-dashed border-[var(--line)] p-10 text-center text-sm text-[var(--muted)]">No tenés avisos nuevos.</div>}</div></>}
