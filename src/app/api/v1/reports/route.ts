import { z } from "zod";import { createReport } from "@/data/community";import { getServerSession } from "@/lib/auth-server";import { apiData,apiError,requestId } from "@/lib/server/api-response";import { checkRateLimit,visitorKey } from "@/lib/server/rate-limit";
const schema=z.object({targetType:z.enum(["profile","service","review"]),targetId:z.string().min(1).max(100),reason:z.enum(["informacion_falsa","contenido_inapropiado","estafa","spam","otro"]),description:z.string().trim().max(800).default("")});
export async function POST(request:Request){const id=requestId(request);const visitor=visitorKey(request);const limit=await checkRateLimit("report",visitor,5,3600);if(!limit.allowed)return apiError("RATE_LIMITED","Alcanzaste el límite temporal de reportes.",429,id);const parsed=schema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return apiError("VALIDATION_ERROR","Revisá el reporte.",422,id,z.flattenError(parsed.error).fieldErrors);const session=await getServerSession();return apiData(await createReport({...parsed.data,reporterUserId:session?.user.id,visitorHash:visitor}),{requestId:id},{status:201});}
