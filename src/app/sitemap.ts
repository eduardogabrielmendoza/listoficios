import type { MetadataRoute } from "next";import { categories,professionals } from "@/lib/mock-data";import { BELLA_VISTA_ZONES } from "@/lib/profile-defaults";import { createSlug } from "@/lib/slug";
export default function sitemap():MetadataRoute.Sitemap{const base=process.env.APP_URL??"http://localhost:3000";const now=new Date();const pages=["","/profesionales","/servicios","/zonas","/como-funciona","/seguridad","/ayuda","/nosotros","/privacidad","/terminos"];return[...pages.map((path)=>({url:`${base}${path}`,lastModified:now,changeFrequency:path===""?"daily" as const:"weekly" as const,priority:path===""?1:.7})),...categories.map((category)=>({url:`${base}/servicios/${category.id}`,lastModified:now,changeFrequency:"weekly" as const,priority:.8})),...BELLA_VISTA_ZONES.map((zone)=>({url:`${base}/zonas/${createSlug(zone)}`,lastModified:now,changeFrequency:"weekly" as const,priority:.7})),...professionals.map((profile)=>({url:`${base}/profesionales/${profile.slug}`,lastModified:now,changeFrequency:"weekly" as const,priority:.8}))]}
